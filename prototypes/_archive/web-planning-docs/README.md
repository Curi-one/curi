# Archived planning docs (web prototype era)

**Superseded.** Do not use for implementation.

Canonical spec: [`../../docs/`](../../docs/) at repo root.

These files were written during early Vite prototype planning (Claude API, old schema). Kept for historical reference only.
